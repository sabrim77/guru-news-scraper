export default {
  plugins: {
    'tailwindcss/nesting': {},
    tailwindcss: {},
    autoprefixer: {},
    // Optional: enable modern CSS features (nesting, media ranges, etc.)
    // 'postcss-preset-env': { stage: 3 },
  },
}
